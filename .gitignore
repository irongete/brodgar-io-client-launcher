/build
# a development run in this folder leaves these behind
/client
/cache
/launcher.properties
/client.log
/build.properties
# the JDK the runtime is cut from, the OpenJFX jmods that go into it and the OpenJFX SDK the Workshop item
# carries, unpacked here and never committed
/jdk-*
/javafx-jmods-*
/javafx-sdk-*
